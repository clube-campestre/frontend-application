name: Build and Deploy Frontend

on:
  push:
    branches: [ "develop_v2" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Checkout do código
      - name: Checkout
        uses: actions/checkout@v4

      # QEMU para multi-plataforma (opcional mas recomendado)
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      # Buildx (necessário para docker/build-push-action)
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Login no Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Build e Push usando ação oficial do Docker
      - name: Build and Push Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/desbravadores-frontend:latest

      # Configure SSH agent with private keys (recommended)
      - name: Start ssh-agent and add private keys
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: |
            ${{ secrets.DESBRAVADORES_SSH_KEY }}

      # Add bastion and target host keys to known_hosts to avoid host verification errors
      - name: Add remote hosts to known_hosts
        run: |
          mkdir -p ~/.ssh
          touch ~/.ssh/known_hosts
          ssh-keyscan -H ${{ secrets.BASTION_PUBLIC_IP }} >> ~/.ssh/known_hosts || true
          ssh-keyscan -H ${{ secrets.FRONT_PRIVATE_IP }} >> ~/.ssh/known_hosts || true
          chmod 644 ~/.ssh/known_hosts

      - name: Deploy via docker-compose (using Bastion)
        run: |
          ssh -o StrictHostKeyChecking=no \
              -o ProxyJump=ubuntu@${{ secrets.BASTION_PUBLIC_IP }} \
              -i ~/.ssh/private.pem \
              ubuntu@${{ secrets.FRONT_PRIVATE_IP }} \
              "cd /home/ubuntu/frontend && docker compose pull && docker compose up -d"
